<?xml version="1.0" encoding="utf-8"?>
<!--
  1x1 icon-only widget. A single phone-handset icon whose colour reflects the last known
  call-forwarding state (green = on, red = off). Tapping toggles the same action as the 2x1 widget.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background_neutral"
    android:padding="8dp">

    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:adjustViewBounds="true"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_phone"
        tools:tint="@color/phone_on" />

</FrameLayout>
